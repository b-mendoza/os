# Verification

- Run `pnpm run lint` after meaningful code changes.
- Run `pnpm run test` when changing behavior or adding tests.
- Run `pnpm run build` when touching app wiring, routing, or build configuration.

## Reference

- `pnpm run lint` includes formatting, linting, and type checks.
- Standalone typecheck: `pnpm run lint:types`.
